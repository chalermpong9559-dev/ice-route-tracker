# สุรินทร์น้ำทิพย์ — Ice Route Tracker

## ภาพรวมโปรเจกต์

เว็บแอปสำหรับบันทึกและจัดการสายการขายน้ำแข็ง **26 สาย** ของโรงน้ำแข็งสุรินทร์น้ำทิพย์
เป้าหมายหลัก: ให้ใครก็ได้สามารถวิ่งสายแทนคนขับเดิมได้ โดยไม่ต้องพึ่งความจำของคนขับ

**Live site:** https://chalermpong9559-dev.github.io/ice-route-tracker/

---

## ระบบที่มีอยู่แล้ว (ห้ามแตะโดยไม่จำเป็น)

### 1. บันทึกสาย (Master Data)
- จัดการสาย 26 สาย (เพิ่ม/แก้ไข/ลบ/เรียงลำดับ)
- บันทึกข้อมูลร้านค้าในแต่ละสาย: ชื่อร้าน, ราคา/ถุง, ประเภทน้ำแข็ง, จำนวน
- ถ่ายรูปร้าน + ปักหมุด GPS
- คำนวณยอดรวมอัตโนมัติ + โปรแถม (ซื้อ 10 แถม 1)
- Export CSV / สรุปรายงาน / แสดงเส้นทางบนแผนที่
- Supabase tables: `routes`, `shops`

### 2. วิ่งสาย (Daily Recording)
- โหลด template ร้านจากสายเป็น starting point
- แก้ไขจำนวนถุงต่อร้านได้ตามความเป็นจริงของวันนั้น
- เพิ่มร้านใหม่ที่ไม่อยู่ใน template ได้
- กรอกชื่อคนขับ, ข้าม/ย้อนกลับได้
- บันทึกลง Supabase เมื่อจบสาย
- Supabase tables: `run_sessions`, `run_items`

### 3. ประวัติวิ่งสาย
- ดูประวัติการวิ่งสายรายเดือน กรองตามสาย
- สรุปยอดถุง+เงินรายเดือน
- กดขยายดูรายร้านในแต่ละรอบได้

### 4. บันทึกยอด (Daily Log)
- บันทึกยอดขายระดับสาย: ถุง, เงินที่เก็บได้จริง, ค่าใช้จ่าย (น้ำมัน/ค่าเด็ก/อื่นๆ)
- คำนวณรายได้สุทธิ, เช็คขาด/เกิน
- ดูรายงานรายวัน/รายเดือน
- Supabase table: `daily_logs`

---

## Supabase Tables

| Table | ใช้ทำอะไร |
|-------|-----------|
| `routes` | รายชื่อสาย |
| `shops` | ร้านค้าใน template แต่ละสาย |
| `run_sessions` | หัว record การวิ่งสายแต่ละครั้ง |
| `run_items` | รายละเอียดร้านต่อ session |
| `daily_logs` | บันทึกยอดรายวันระดับสาย |

---

## Tech Stack

- **Frontend:** HTML5, CSS, Vanilla JavaScript — ไฟล์เดียว (`index.html`)
- **Backend:** Supabase (PostgreSQL + Storage) — REST API, anon key ฝังใน HTML
- **Hosting:** GitHub Pages (`chalermpong9559-dev` org)
- **Maps:** Leaflet.js (OpenStreetMap) + Google Maps นำทาง

## ประเภทน้ำแข็ง

| key | ชื่อ | icon |
|-----|------|------|
| `ts` | หลอดเล็ก | 🔵 |
| `tl` | หลอดใหญ่ | 🔷 |
| `cc` | บดหยาบ | 🌨️ |
| `cf` | บดละเอียด | ❄️ |

---

## workflow ที่ต้องทำทุกครั้ง

> **แก้โค้ดเสร็จแล้ว ให้ `git add . && git commit && git push` ขึ้น GitHub ทันทีเสมอ**
> ไม่ต้องรอให้ user สั่ง

---

## แนวทางการพัฒนา

- UI ภาษาไทย, mobile-first — คนขับที่ไม่ถนัดเทคโนโลยีต้องใช้ได้
- ไม่ต้องติดตั้งแอป — เปิดผ่านเบราว์เซอร์ได้เลย
- **ยังไม่มี login** — อย่าเพิ่งทำ รอสูตรคิดเงินเดือนก่อน
- RLS ปิดไว้ทุกตาราง (เปิดเมื่อมี login)

---

## สิ่งที่ต้องพัฒนาต่อ

- [ ] บันทึกเส้นทางทั้ง 26 สายให้ครบ
- [ ] สูตรคิดเงินเดือนคนขับ (รอ user ยืนยัน)
- [ ] ระบบ login + Row Level Security
